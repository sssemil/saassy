# dokustatus

Rust (Axum + SQLx) backend with a Next.js UI to let users track German ID/pass statuses via the Munich endpoint. Auth is passwordless magic links, with Redis-backed rate limits and Postgres persistence.

This project is 95% AI-slop.

## Notices
- Licensed under MIT (see `LICENSE`).
